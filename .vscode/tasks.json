{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "${workspaceFolder}/run_build.sh"
        },
        {
            "label": "test",
            "type": "shell",
            "command": "${workspaceFolder}/run_build.sh && ${workspaceFolder}/build/tests/tests",
            "problemMatcher": [],
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}